import React from 'react';
import Home from '../src/screens/Home';
import './App.css';

const app = () => {
    return (
        <React.Fragment>
            <Home />
        </React.Fragment>
    );
};

export default app;
